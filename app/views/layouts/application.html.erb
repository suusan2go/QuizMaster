<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>
    <title>QuizMaster</title>
    <%= stylesheet_link_tag webpack_asset_path('bundle.css') %>
  </head>

  <body>
    <div class="app-root">
      <% #TODO serialize %>
      <%= render_react_component('AppRouter', location: request.url, current_user: current_user.as_json) %>
    </div>
    <%= javascript_include_tag webpack_asset_path('bundle.js') %>
  </body>
</html>
